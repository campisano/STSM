language: cpp
compiler: gcc
os: linux
dist: trusty
addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-7
            - make
script:
    - export CC="g++-7"
    - make -e test
    - ./BUILD/test/stsm
